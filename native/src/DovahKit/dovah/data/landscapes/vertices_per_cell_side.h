#pragma once

namespace dovah::landscapes {
   // Vertices per cell side, including the vertex that is part of the shared edge between 
   // two adjacent landscapes.
   constexpr const size_t vertices_per_cell_side = 33;
}