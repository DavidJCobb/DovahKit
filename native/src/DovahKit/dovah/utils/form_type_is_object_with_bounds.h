#pragma once
#include "../form_types.h"

namespace dovah {
   // Returns true if the given form type is a subclass of TESBoundObject, basically.
   extern bool form_type_is_object_with_bounds(form_type);
}
