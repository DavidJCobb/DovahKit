#pragma once
#include <QString>
#include "dovah/data/packages/interrupt_override_target.h"

namespace editor::localize {
   extern QString package_interrupt_override_target(dovah::packages::interrupt_override_target);
}