#pragma once
#include <QString>
#include "dovah/data/packages/interrupt_override_type.h"

namespace editor::localize {
   extern QString package_interrupt_override_type(dovah::packages::interrupt_override_type);
}